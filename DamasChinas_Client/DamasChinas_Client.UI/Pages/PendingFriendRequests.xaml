<Page x:Class="DamasChinas_Client.UI.Pages.PendingFriendRequests"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource SurfaceBrush}">

    <Grid>
        <!-- Fondo de madera -->
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/DamasChinas_Client.UI;component/Assets/Backgrounds/board-background.png"
                        Stretch="UniformToFill"/>
        </Grid.Background>

        <Rectangle Fill="#66000000"/>

        <Grid Margin="40">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Width="60" Height="60"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="OnBackClick">
                    <Image Source="pack://application:,,,/DamasChinas_Client.UI;component/Assets/Icons/backIcon.png"
                           Width="40" Height="40"/>
                </Button>

                <TextBlock Grid.Column="1"
                           Text="{DynamicResource pendingFriendRequests}"
                           Foreground="White"
                           FontSize="48"
                           FontFamily="pack://application:,,,/DamasChinas_Client.UI;component/Assets/Fonts/#Oups"
                           TextAlignment="Center"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- LISTA -->
            <Border Grid.Row="1"
                    Background="#80222222"
                    CornerRadius="12"
                    BorderBrush="#444"
                    BorderThickness="1"
                    Padding="24">

                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">

                    <ItemsControl x:Name="RequestsList" ItemsSource="{Binding Requests}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>

                                <Border Background="#44222222"
                                        Margin="0,0,0,12"
                                        Padding="12"
                                        CornerRadius="10"
                                        BorderBrush="#666"
                                        BorderThickness="1">

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Username -->
                                        <TextBlock Text="{Binding Username}"
                                                   Foreground="White"
                                                   FontSize="20"
                                                   VerticalAlignment="Center"/>

                                        <!-- Botón ACEPTAR -->
                                        <Button Grid.Column="1"
                                                Width="120"
                                                Height="40"
                                                Margin="10,0"
                                                Style="{DynamicResource BaseGameButton}"
                                                Content="{DynamicResource acceptRequest}"
                                                Click="OnAcceptClick"/>

                                        <!-- Botón RECHAZAR -->
                                        <Button Grid.Column="2"
                                                Width="120"
                                                Height="40"
                                                Style="{DynamicResource BaseGameButton}"
                                                Content="{DynamicResource rejectRequest}"
                                                Click="OnRejectClick"/>

                                    </Grid>

                                </Border>

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </ScrollViewer>

            </Border>

        </Grid>
    </Grid>
</Page>

